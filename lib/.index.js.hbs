/**
 * {{pkg.description}}
 * @module {{pkg.name}}
 * @version {{pkg.version}}
 */

'use strict'

const filecopy = require('./filecopy')

const lib = filecopy.bind(this)

Object.assign(lib, {
  filecopy
})

module.exports = lib
